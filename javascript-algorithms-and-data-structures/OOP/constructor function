/**
 * Constructors are functions that create new objects. They define 
 * properties and behaviors that will belong to the new object. 
 * Think of them as a blueprint for the creation of new objects.
 */

function Bird() {
  this.name = "Albert";
  this.color = "blue";
  this.numLegs = 2;
}
